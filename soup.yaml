base: Qwen/Qwen2.5-1.5B-Instruct

task: sft

backend: mlx

data:
  train: ./data/train.jsonl
  format: alpaca
  val_split: 0.1

training:
  epochs: 2
  lr: 2e-5
  batch_size: auto
  lora:
    r: 8
    alpha: 16

output: ./output